# requirements.txt - Comprehensive list for both FastAPI backend and Streamlit frontend

# Web Frameworks
fastapi>=0.111.0
uvicorn>=0.30.1 # ASGI server for FastAPI
pydantic>=2.7.4 # For FastAPI schemas (BaseModel) and data validation

# Streamlit UI
streamlit>=1.28.1

# Core RAG/AI Libraries
langchain>=0.2.0
langchain-community>=0.2.0
langchain-core>=0.2.0
langchain-google-genai>=1.0.0 # Gemini LLM integration

# Vector Store and Embeddings
chromadb==0.4.18
sentence-transformers==2.7.0 # For the "all-MiniLM-L6-v2" embedding model

# Data Handling & Utilities
pandas>=2.1.3
sqlite-utils==3.35.2 # For storing metadata in SQLite
python-dotenv==1.0.0 # For loading environment variables (.env file)
Faker>=25.1.0 # For generating mock data (in data_generator.py)